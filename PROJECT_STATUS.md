# NetDebugger 项目状态归档

> 本文档记录 NetDebugger 的当前状态、已实现功能、待办事项及技术债务。开发规则见 [AGENTS.md](AGENTS.md)。
> 最后更新：2026-08-27（应用图标更换为 49 透明六边形轨道）

---

## 1. 项目概况

**名称**：NetDebugger
**定位**：跨平台网络协议调试器（Tauri v2 + Rust + 原生 HTML/CSS/JS），当前以 WebSocket 调试为主

**技术栈**：Tauri v2 · Tokio · tokio-tungstenite · SQLite (tokio-rusqlite) · tauri-plugin-store · tauri-plugin-single-instance · tauri-plugin-updater

**版本**：单点维护于 `src-tauri/Cargo.toml`（`version` 字段）；标题栏 / 欢迎页 / 关于页 / 构建产物名（`NetDebugger_v<版本>.exe`）统一读取

---

## 2. 已实现功能

### 2.1 核心调试

- [x] WS 服务端 / 客户端模拟；WS Server 多客户端 + 多 endpoint 路径（握手按路径路由、未知路径 404、按 endpoint 定向广播）
- [x] 消息发送目标：服务端支持全体广播 / 某 endpoint 全体广播 / 指定客户端（当前仅支持发送文本）；客户端仅向连接的服务端发送，无目标选择
- [x] 消息发送格式：文本 / Hex / Base64 三种模式切换（Hex/Base64 解码为原始字节后按二进制帧发送），服务端支持按目标广播
- [x] 发送前 JSON 自动格式化：文本模式下内容为完整 JSON（对象/数组）时自动缩进后发送
- [x] 清空消息记录快捷键：全局 `Ctrl+L`（带确认框，Enter 确认 / Esc 取消）
- [x] 导出消息历史：时间线工具栏导出按钮，弹框选择 JSON / 文本格式，系统保存对话框写入本地（以数据库为数据源，含全部历史；JSON 中非 UTF-8 载荷附 base64 字段）
- [x] 客户端自定义请求头（握手时发送，如 Authorization / Origin / X-Foo）与 Subprotocol（`Sec-WebSocket-Protocol` 多选，逗号分隔合并）
- [x] 项目分组管理；会话可独立或归组；连接自定义命名、编辑（运行中禁止编辑，协议/角色创建后锁定）；未重命名时默认显示地址（服务端 `0.0.0.0:端口`、客户端目标 URL）
- [x] 客户端自动重连（仅客户端）：会话配置自动重连间隔（秒，默认 30，0 关闭），断开后按间隔自动重连并持续重试，手动停止时退出；连接成功后重连次数清零
- [x] Ping / Pong 控制帧与心跳监控：发送区 Ping 按钮（客户端 ping 服务端、服务端广播所有客户端，无客户端时禁用），手动 Ping 在时间线显示往返延迟；客户端可配置自动心跳间隔（秒，0 关闭）；发送区实时显示延迟/Pong 时间/心跳超时（标红）；修复服务端 Ping 无反馈
- [x] 发送区与状态一致性：运行时状态缓存避免 `loadProjects` 覆盖最新 `running` 状态；发送框选中会话后即启用（未连接也可预输入，按会话隔离草稿 `state.sendDrafts`），服务端运行时发送按钮即放开（无客户端仍可广播），Ping 仍按无客户端禁用
- [x] 自动响应规则（优雅实现，按会话配置）：sessions 表 `auto_replies` 列，支持包含/精确/正则匹配 → 文本/Hex/Base64 回复 + 延迟；预编译正则，首条命中即自动回复，时间线提示 `↻ 自动回复` 置于气泡上方且与气泡共用同一时间戳（以气泡时间为准），仅服务端生效，支持运行时动态更新
- [x] 客户端一个连接对应一个 endpoint（目标地址只填 host:port，endpoint 拼接到 URL）
- [x] 服务端监听地址只填端口，自动绑定 `0.0.0.0:端口`（连接树显示 `WS Server 0.0.0.0:port`）
- [x] 消息时间线（文本 / JSON 高亮 / 十六进制详情）、复制按钮、消息搜索与高亮、历史持久化；上滑到顶部自动加载更早历史（分页，默认一次加载 100 条）
- [x] 未读角标按 endpoint 分桶；endpoint 作为左侧连接树子节点（折叠/展开、点击只看该路径、连接角标为各 endpoint 之和）
- [x] 服务端收到的消息气泡显示发送者（重命名客户端后显示名称，否则显示 IP 端口，重启后仍保留）
- [x] 连接状态提示：连接/断开事件以灰色小字居中显示在消息时间线中（服务端启动/断开、客户端连上/断开、客户端侧连接成功/断开），并随消息历史持久化
- [x] 数据库自动瘦身：启动时若空闲页占比 > 25% 自动 VACUUM 压缩文件（清空大量消息后文件不长期停在历史最高水位）

### 2.2 交互体验

- [x] 无边框窗口 + 自定义标题栏（拖拽、双击最大化、最小化/最大化/关闭按钮）
- [x] 左侧连接 S/C 角色徽标 + WS 协议标签；启动/停止按钮常显（绿/红）
- [x] 连接编辑/删除改为右键菜单（删除红色、运行中禁用）；运行中禁止删除
- [x] 分组编辑（重命名）/删除同样改为右键菜单（删除红色），分组头仅保留"+"添加连接
- [x] 点击已选中的连接/endpoint 再次点击取消选择回到欢迎页
- [x] 分栏拖拽调节宽度/高度并持久化（localStorage）
- [x] 主题下拉切换（系统/浅色/深色），设置弹框（通用 / 快捷键 / 关于 左侧栏）
- [x] 自定义弹框组件（确认/输入/toast），危险操作确认按钮红色；无浏览器默认弹框；确认框支持 Enter 确认 / Esc 取消
- [x] 常用消息模板 / 快捷指令：全局侧边栏闪电图标入口（原发送栏入口已移除），Notion 式下拉（条目悬浮显示编辑/删除按钮），新增与编辑弹编辑框（可拖动移动 / 8 向调整大小），localStorage 持久化；侧边栏 `新建分组/新建连接` 改为图标按钮（文件夹+号/链路）
- [x] 端口占用 / 连接失败 toast 友好提示；禁用浏览器右键菜单
- [x] 系统托盘（左键单击显示/隐藏窗口、无白块）、关闭确认、窗口大小持久化
- [x] 单例运行：只允许一个进程，再次启动直接激活（显示 + 聚焦）已有窗口
- [x] 启动无白屏（窗口初始隐藏 `visible:false`，前端就绪后 `show()`），窗口背景色随主题
- [x] 关闭按钮遵循"关闭后最小化到系统托盘"设置（勾选则直接隐藏，否则弹确认框）
- [x] 消息列表长消息截断开关（工具栏图标切换）；自定义即时悬浮提示（悬停立即显示按钮说明）
- [x] 自定义滚动条样式（细条圆角、颜色随主题），空详情栏不显示滚动条
- [x] 标题栏集成：检查更新（旋转加载动画 + 红点角标）、切换主题、设置按钮，均位于最小化左侧
- [x] 侧边栏工具栏："+ 分组"和"+ 连接"按钮置于左侧连接列表顶部
- [x] 发送区清空按钮：垃圾桶图标，鼠标悬停变红，与清空消息记录按钮样式一致
- [x] 标题栏拖拽与交互元素互不干扰：主题下拉、快捷指令下拉等弹出层不会触发窗口拖动
- [x] 消息气泡右键菜单（独立组件，样式与连接菜单一致）：再次发送（文本消息）直接经发送框重发；删除单条消息（同步清理数据库记录）
- [x] 再次发送：把消息内容填入发送框，复用当前发送目标（更贴合实际调试习惯）；原"消息重发（回原目标）"需求已由再次发送覆盖
- [x] 消息气泡收/发区分：接收消息淡蓝底 + 左侧蓝色条、发送消息淡绿底 + 右侧绿色条（宽度 4px），通过底色与色条区分收发
- [x] JSON 树形折叠视图：详情 `JSON` 标签下 `源码 | 树形` 子标签，`▼/▶` 折叠（深度≥2 默认收起），支持搜索高亮与源码复制
- [x] 高级过滤：时间线漏斗按钮展开面板，支持按方向/类型/字节大小/正则（独立输入框）/JSONPath 过滤；正则与 JSONPath 独立于搜索框，纯前端高效实现
- [x] 一键复制为 ...：消息气泡右键 `复制为 ...` 子菜单（Raw/Hex/Base64/Escaped/cURL，WS 显示 cURL）+ 详情面板 `▾` 下拉，协议自适应，JSON 自动格式化后参与 cURL/Diff
- [x] 消息对比 Diff：气泡右键 `选择为对比基准 / 与基准对比`，基准虚线高亮 + 角标，弹框并排按行 LCS 高亮差异（行级 delete/insert），支持交换与滚动
- [x] 批量/压测发送：气泡右键 `批量发送...` 弹框填次数/间隔循环发送，复用原消息载荷与当前发送目标，服务端无客户端时禁用，支持停止与进度提示；仅点击取消/开始关闭对话框
- [x] 服务端自动回复并发化：`ws.rs` 接收循环改为 `spawn` 独立任务处理延迟与发送，避免阻塞后续消息接收（100ms×5 场景）
- [x] 流量统计面板：时间线工具栏 `📈` 按钮切换 `总计 | ↑ | ↓ | 速率(合计) | ↑峰值 | ↓峰值` 双列独立峰值，彩色 SVG 箭头（↑绿/↓蓝），1s 滑窗后台采样即使隐藏/未选中也更新峰值
- [x] 收藏/标星消息：气泡右键 `收藏/取消收藏`（`messages.pinned` 落库 `012_pinned.sql`，清空时 `DELETE ... pinned=0` 保留），时间线 `★` 角标+边框；工具栏 `★ 已收藏` 弹框 DB 全量 `load_pinned_messages`（修复分页未加载导致不显示）+ 右侧 `定位/取消` 即时悬浮提示（`dialog top-layer` 适配）+ 搜索过滤（内容/端点/方向，高亮）+ 点击进纯预览（`get_message_by_id` 回补）并在预览全文高亮搜索词，定位时未加载消息自动回插时间线
- [x] 详情内搜索：详情面板 `Ctrl+F` 呼出搜索条（输入 + 计数 `n/m` + ↑↓/✕），文本/JSON源码/树形/Hex 全视图高亮（优先于时间线搜索，`mark.current` 橙色定位），树形搜索时自动展开折叠节点，支持 Enter/Shift+Enter 与 Esc 关闭
- [x] 发送框按会话隔离：`state.sendDrafts` Map 按 `sessionId` 保存草稿，切换会话时自动保存/恢复，发送/清空/模板插入同步更新，删除会话时清理
- [x] 时间线批量选择：工具栏 `批量选择` 切批量模式（消息与提示语均可勾选，`全选/indeterminate/已选 n/退出`），批量时右上角 `导出/清空` 仅处理已选（导出为 JSON/文本前端按已选生成并下载，清空逐条 `delete_message` 并前端过滤），切换会话按会话隔离清空已选

### 2.3 自动更新与 CI/CD

- [x] GitHub Actions 自动构建：推送 `v*` 标签触发 Windows 单平台构建并发布到 GitHub Releases（macOS/Linux 已注释，待项目成熟后恢复）
- [x] Tauri 签名更新：生成签名密钥，构建产物自动签名，`latest.json` 包含各平台签名信息
- [x] 应用内自动更新（tauri-plugin-updater）：启动时静默检查 + 每 30 分钟定时检查；手动检查时标题栏图标旋转加载，有更新显示红点角标
- [x] 更新弹框：已是最新显示当前版本号 + 关闭按钮；发现新版本显示版本对比 + 更新按钮，点击自动下载安装

---

## 3. 已知限制

- 仅支持 WebSocket 协议
- 单元测试覆盖有限（仅 `resolve_endpoint` 的 4 个单测）
- 停止 WS Server 后端口可能不立即释放（依赖操作系统 `TIME_WAIT`）

---

## 4. 待开发功能

> 以下各条均附大概实现方案；「迁移」指 src-tauri/migrations 新增 SQL 文件并注册到 db.rs 的 MIGRATIONS 数组。

### 高优先级

（暂无；详见下方中/低优先级）

### 中优先级

**通用能力（跨协议复用，WS / MQTT / TCP 均可用）**

- [x] **一键复制为 ...**：`Copy as cURL / Hex / Base64 / Escaped String / Raw bytes`（协议相关文案自动适配：WS 显示 cURL，MQTT 显示 Publish 命令等）
- [x] **固定/标星消息**：Pin 重要消息置顶，清空时保留（已实现为收藏，见 2.2）
- [x] **批量/压测发送**：选一条消息填次数与间隔循环发送，用于复现和压测
- [x] **流量统计面板**：当前会话消息数、上行/下行字节、速率（msg/s, KB/s）、峰值
- [x] **消息对比 Diff**：选两条消息并排高亮差异
- [x] **详情内搜索**：详情面板（文本/JSON 树）内 `Ctrl+F` 搜索（已实现，见 2.2）
- [ ] **持续落盘日志**：可选自动追加写入本地文件（按会话/按日切分），长时间抓包不丢
- [ ] **环境变量/模板变量**：`{{host}}`, `{{token}}` 在目标地址/请求头/消息体中替换
- [ ] **工作区导入导出**：分组+连接配置导出为 JSON 文件，团队共享、一键恢复

### 低优先级 / 未来协议

- [ ] 消息收发延迟显示：前端渲染时展示相邻消息时间差
- [ ] MQTT Client / Server（Topic 订阅、QoS/Retain、心跳 KeepAlive 适配通用能力中的过滤/复制/统计等）
- [ ] TCP Client / Server（无 Topic/广播概念，过滤与发送按原始字节，复用通用能力）

---

## 5. 技术债务

- `app.js` 状态与渲染耦合较紧，后续加复杂功能可考虑引入轻量状态管理。
- `ws.rs` 中 server/client 的 socket loop 可进一步拆分。

---

## 6. 备注

- 主题持久化键：`theme`（`system` / `light` / `dark`）
- 最小化到托盘键：`minimize-to-tray`（布尔）
- 窗口大小键：`window-size`；分栏布局键：`netdebugger.layout`（localStorage）
- 配置 / 数据库位置：Tauri 应用本地数据目录（identifier 为 `top.imzz.netdebugger`）
